/.quarto/
**/*.quarto_ipynb

# Rendered preview outputs (see `_preview.R`). The .qmd files are the only
# sources; anything below is generated by `quarto preview`/`render` and must
# stay out of both git and the package build (see ../../.Rbuildignore).
*.html
*_files/
*.knit.md
